/* A program to perform finding the maximum number of the three. */

int max(int x, int y, int z)
{
	int result;
	if (x > y+)
	{
		result = x;
		if (z > result)
		{
			result = z;
		}
	}
	else
	{
		result = y;
		if (z > result)
		{
			result = z;
		}
	}
	return result;
}
